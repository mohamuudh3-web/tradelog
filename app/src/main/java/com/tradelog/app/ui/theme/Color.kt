package com.tradelog.app.ui.theme

import androidx.compose.ui.graphics.Color

val Teal = Color(0xFF12D6A0)      // legacy brand mint (kept for any stray refs)
val TealDark = Color(0xFF0FB389)
val Periwinkle = Color(0xFF5C7CFF)
val Red = Color(0xFFEF4444)
val Amber = Color(0xFFF5A623)

// Primary accent — the single royal blue from the reference UI.
val Blue = Color(0xFF2563EB)
val BlueDark = Color(0xFF1D4ED8)
val BlueSoft = Color(0xFFEAF0FE)  // tinted blue for nav indicator / containers

val Win = Color(0xFF16A34A)       // gain green
val Loss = Color(0xFFEF4444)      // loss red
val Neutral = Color(0xFF6B7280)   // BE grey

// Dark scheme (retained, but app defaults to light to match the reference)
val DarkBg = Color(0xFF0F1722)
val DarkSurface = Color(0xFF18222F)
val DarkSurfaceVariant = Color(0xFF22303F)
val DarkOnSurface = Color(0xFFE6EDF3)

// Light scheme — clean fintech white
val LightBg = Color(0xFFF5F6F8)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFF1F4F8)
val LightOnSurface = Color(0xFF111827)
